name: di_generator_build_example
description: Example application demonstrating the usage of di_generator_build package.
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  get_it: ^7.6.0
  di_generator_build:
    path: ../

dev_dependencies:
  build_runner: ^2.4.0
  build_test: ^2.1.0
  test: ^1.24.0
